/**
 * 
 * Allows the player to fire bullets at enemies.
 */
public interface Weapon {
	/**
	 * 
	 * Fires a bullet from the player's position
	 *
	 * @param board
	 *            The board on which the bullet is fired from
	 * @param x
	 *            The x-value of the position from which the bullet is fired.
	 * @param y
	 *            The y-value of the position from which the bullet is fired.
	 */
	public void fire(Board board, int x, int y);
}
